# Minecraftuuuum NeoForge 26.1 runtime

Official Mojang names, Java 25, ModDevGradle 2.0.141.

```text
gradlew.bat runClient
```

Command: `/minecraftuuuum run {P:say|text=Minecraftuuuum!} {P:place|registry-id=minecraft:oak_planks} {P:spawn|registry-id=minecraft:creeper}`

Lemma packs: `data/minecraftuuuum/lemmas/` (vanilla) and `data/examplewrap/lemmas/` (Create wrap — does not vendor Create).
